#include <iostream>

using namespace std;

class NumberPackage {
	private: int data;
	public: 
		void init(int );
		int factorial(int );
		int fibonacii(int );
		int sigma(int );
		int sigma2(int );
		int sigma3(int );
		int getVal();
};
	